import React from 'react';

export default function Spinner() {
    return (
        <div className="spinner">
            <div className="jenkins-spinner" aria-label="working ..." />
        </div>
    );
}

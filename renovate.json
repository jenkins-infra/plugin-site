{
    "extends": [
        "config:base"
    ],
    "schedule": [
        "every weekday"
    ],
    "labels": ["dependencies"],
    "packageRules": [
        {
            "matchPackageNames": ["p-queue", "husky"],
            "enabled": false
        },
        {
            "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
            "automerge": true
        }
    ]
}
